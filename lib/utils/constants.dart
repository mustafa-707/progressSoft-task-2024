const double pAppBarHeight = 66.0;

const double pAppRadius = 15.0;

const double pButtonRadius = 12.0;

const Duration pOnBoardingAnimationDuration = Duration(seconds: 5);

const Duration pPageTransitionDuration = Duration(milliseconds: 300);

const double pAppNavBarHeight = 72.0;
